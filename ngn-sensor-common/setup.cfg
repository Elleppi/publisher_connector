[metadata]
name = ngn-sensor-common
description = NGN Sensor Common
python_requires = >=3.8

[options]
packages = find:
install_requires =
    redis~=5.2

[options.extras_require]
test =
    pytest
    pytest-cov

[tool:pytest]
pythonpath = ../src

[options.packages.find]
where = src
